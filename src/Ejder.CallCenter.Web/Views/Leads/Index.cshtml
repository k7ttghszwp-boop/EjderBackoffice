@model IEnumerable<Ejder.Core.Models.Reservation>

<h1>📞 Call Center – Lead Listesi</h1>

<table style="width:100%; border-collapse:collapse;">
    <thead>
        <tr>
            <th>Müşteri</th>
            <th>Telefon</th>
            <th>Tur</th>
            <th>Kişi</th>
            <th>Tarih</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
    @foreach (var l in Model)
    {
        <tr style="border-top:1px solid #eee;">
            <td>@l.CustomerName</td>
            <td>@l.Phone</td>
            <td>@l.TourName</td>
            <td>@l.PersonCount</td>
            <td>@l.TourDate?.ToString("dd.MM.yyyy")</td>
            <td>
                <a href="tel:@l.Phone"
                   style="padding:6px 10px; background:#111; color:#fff; text-decoration:none;">
                    Ara 📞
                </a>
            </td>
        </tr>
    }
    </tbody>
</table>
